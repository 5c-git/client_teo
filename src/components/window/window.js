import {
  initSelect,
  initDeliveryDate,
} from '../trade-card/trade-card';

import tradeActionInit from '../trade-action/trade-action';

import {
  shuffle,
  numberSplitter,
} from '../../utils/utils';

window.Corners5ProjectLayout = {
  initSelect,
  initDeliveryDate,
  shuffle,
  numberSplitter,
  tradeActionInit,
};
